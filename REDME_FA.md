# LamookCrypto  
یک ابزار ساده برای **رمزنگاری و رمزگشایی متن** بر اساس یک الگوریتم سفارشی عددی با دو کلید.

---

## ✨ معرفی  
LamookCrypto یک ابزار رمزنگاری است که با یک الگوریتم اختصاصی کار می‌کند.  
در این روش، هر حرف و کاراکتر یک **عدد سه‌رقمی ثابت** دارد و برای رمزنگاری نهایی، از **دو کلید مختلف** استفاده می‌شود:

1. **کلید اول:** کاملاً تصادفی  
2. **کلید دوم:** تصادفی اما با یک‌سری محاسبات خاص که توسط الگوریتم ساخته می‌شود  

این کلیدها روی اعداد اثر گذاشته و رشته‌ای رمزنگاری‌شده تولید می‌کنند. همین روند به صورت معکوس برای رمزگشایی استفاده می‌شود.

---

## 🔥 ویژگی‌ها
- 🔐 **رمزنگاری متن** با دو کلید  
- 🔓 **رمزگشایی متن رمز شده**  
- 🎯 الگوریتم عددی ساده و کلاسیک  
- 🔑 دو کلید متفاوت (Random Key + Computed Random Key)  
- ⌨️ پشتیبانی از حروف انگلیسی، اعداد و چند علامت پرکاربرد  
- 🖥️ رابط کاربری ساخته‌شده با CustomTkinter  
- 📂 ساختار ساده و قابل توسعه  

---

## 📁 ساختار پروژه
```
LamookCrypto/
│
├── core/
│   └── cipher_core.py     # شامل الگوریتم، کلیدها، جدول کاراکترها و توابع اصلی
│
└── gui/
    └── app.py             # رابط کاربری برنامه (برای اجرا)
```

---

## 🚀 نحوه اجرا
### 1. نصب پیش‌نیاز  
```bash
pip install customtkinter
```

### 2. اجرای برنامه  
```bash
cd gui
python app.py
```

---

## 🧠 خلاصه‌ی الگوریتم  
- هر کاراکتر → یک عدد سه‌رقمی پایه  
- کلید اول → تصادفی  
- کلید دوم → تصادفی + محاسبات اختصاصی  
- ترکیب این کلیدها → ایجاد خروجی رمزنگاری‌شده  
- رمزگشایی → انجام مراحل معکوس

---

## 📜 لایسنس – MIT License  
استفاده، کپی، تغییر و انتشار این پروژه برای همه مجاز است،  
به شرط اینکه نام سازنده ذکر شود و هیچ مسئولیتی متوجه نویسنده نباشد.

---

## 👤 نویسنده  
**Mahdiyar (mmlamooki8809)**  
